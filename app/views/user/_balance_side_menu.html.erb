<%= 

    menu_items = [['total', 'Total', '/user/balance_over_time'], 
                  ['with-friends', 'With Friends', '/user/balances_over_time_with_friends'],
                  ['more', 'More', '#'],
                  ['options', 'Options', '#']].map do |id, name, link|
        result = {id: id, name: name, link: link}
        if balance_side_menu.to_s == id
            result[:active] = true
        end
        result
    end

    render partial: 'user/side_menu', object: menu_items

%>