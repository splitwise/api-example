<script type="text/javascript">
    console.debug(this.primeCharts)
    console.debug(<%=raw @data %>);
    this.primeCharts(<%=raw @data %>);
 
    /* Uncomment this to see a funny bug.
    that = this;
    $(document).ready(function () {
        console.debug('I begin to prime the charts.');
        that.primeCharts(<%=raw @data %>);
        console.debug('I have primed the charts.');
    });*/
</script>
